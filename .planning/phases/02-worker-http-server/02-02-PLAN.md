---
phase: 02-worker-http-server
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/codestra-start-worker/SKILL.md
autonomous: true
requirements:
  - WORKER-01
  - WORKER-02

must_haves:
  truths:
    - "La descrizione di $2 nel SKILL.md non dice più 'Ignorare per ora' ma spiega che va passato come workerPort"
    - "Le istruzioni operative includono workerPort come parametro di swarm_register"
    - "L'output all'utente mostra la callback_url invece della nota 'sarà attivo nella Fase 2'"
  artifacts:
    - path: "skills/codestra-start-worker/SKILL.md"
      provides: "Skill aggiornata con istruzioni operative per workerPort"
      contains: "workerPort"
  key_links:
    - from: "skills/codestra-start-worker/SKILL.md"
      to: "servers/mcp-server.mjs → swarm_register(workerPort)"
      via: "istruzioni operative riga 1 della sezione Istruzioni operative"
      pattern: "workerPort"
---

<objective>
Aggiornare `skills/codestra-start-worker/SKILL.md` per attivare il supporto al parametro `$2` (worker-port) ora che `swarm_register` accetta `workerPort`.

Purpose: Il SKILL.md attuale dice "Ignorare per ora se fornito" per `$2` — questa nota era un placeholder per la Fase 2. Ora che `mcp-server.mjs` gestisce `workerPort`, il SKILL.md deve guidare Claude a passarlo correttamente.

Output: 3 modifiche chirurgiche al testo di SKILL.md — nessuna riscrittura del file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Stato attuale di skills/codestra-start-worker/SKILL.md — sezioni da modificare. -->

**Riga 15 attuale (parametro $2):**
```
- Worker Port (`$2`): opzionale — porta per il server HTTP del worker (Fase 2). Ignorare per ora se fornito.
```

**Righe 42-45 attuali (istruzioni operative — lista parametri swarm_register):**
```
1. Usa il tool `swarm_register` con:
   - `role`: `"worker"` (oppure ometti se `SWARM_ROLE` è già configurata nell'env)
   - `task`: breve descrizione del lavoro che questo worker svolgerà (chiedere all'utente se non noto)
```

**Righe 52-54 attuali (output all'utente — ultime due righe della sezione):**
```
- Conferma che il worker è ora registrato e in ascolto di task
- Nota: il parametro worker-port (`$2`) sarà attivo nella Fase 2 (Worker HTTP Server)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aggiorna SKILL.md — attiva workerPort nelle 3 sezioni</name>
  <files>skills/codestra-start-worker/SKILL.md</files>

  <read_first>
    - skills/codestra-start-worker/SKILL.md (leggere il file attuale per verificare lo stato corrente prima di applicare le modifiche — le righe potrebbero essere spostate da fix precedenti)
  </read_first>

  <action>
Applicare esattamente le seguenti 3 modifiche chirurgiche a `skills/codestra-start-worker/SKILL.md`.

**Modifica 1 — Descrizione parametro $2:**

Trovare questa riga (nella sezione Parametri):
```
- Worker Port (`$2`): opzionale — porta per il server HTTP del worker (Fase 2). Ignorare per ora se fornito.
```
Sostituire con:
```
- Worker Port (`$2`): opzionale — porta per il server HTTP del worker. Se fornito, passare come `workerPort` al tool `swarm_register`. Se omesso, l'OS assegna automaticamente una porta libera.
```

**Modifica 2 — Lista parametri di swarm_register nelle istruzioni operative:**

Trovare questo blocco (nella sezione "Istruzioni operative"):
```
1. Usa il tool `swarm_register` con:
   - `role`: `"worker"` (oppure ometti se `SWARM_ROLE` è già configurata nell'env)
   - `task`: breve descrizione del lavoro che questo worker svolgerà (chiedere all'utente se non noto)
```
Sostituire con:
```
1. Usa il tool `swarm_register` con:
   - `role`: `"worker"` (oppure ometti se `SWARM_ROLE` è già configurata nell'env)
   - `task`: breve descrizione del lavoro che questo worker svolgerà (chiedere all'utente se non noto)
   - `workerPort`: se `$2` è fornito, passare il suo valore numerico; altrimenti omettere (l'OS assegnerà la porta)
```

**Modifica 3 — Sezione output all'utente:**

Trovare queste due righe (nella sezione "Output all'utente", ultime due righe del file):
```
- Conferma che il worker è ora registrato e in ascolto di task
- Nota: il parametro worker-port (`$2`) sarà attivo nella Fase 2 (Worker HTTP Server)
```
Sostituire con:
```
- Conferma che il worker è ora registrato e in ascolto di task
- La `callback_url` del worker HTTP server (restituita da `swarm_register` nella risposta)
```

Non modificare nessun'altra parte del file.
  </action>

  <verify>
    <automated>node --input-type=module --eval "
import { readFileSync } from 'node:fs';
const src = readFileSync('./skills/codestra-start-worker/SKILL.md', 'utf8');
const checks = [
  [/Ignorare per ora se fornito/, 'testo obsoleto rimosso', false],
  [/sarà attivo nella Fase 2/, 'nota Fase 2 futura rimossa', false],
  [/workerPort/, 'workerPort presente'],
  [/passare.*workerPort.*al tool/, 'istruzione passaggio workerPort in descrizione $2'],
  [/workerPort.*se.*\\\$2.*è fornito/, 'workerPort in istruzioni operative'],
  [/callback_url.*del worker HTTP server/, 'callback_url in sezione output'],
];
let ok = true;
for (const [re, name, shouldMatch = true] of checks) {
  const found = re.test(src);
  if (shouldMatch && !found) { console.error('MISSING:', name); ok = false; }
  else if (!shouldMatch && found) { console.error('SHOULD BE REMOVED:', name); ok = false; }
  else console.log('OK:', name);
}
process.exit(ok ? 0 : 1);
"
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep "Ignorare per ora" skills/codestra-start-worker/SKILL.md` restituisce 0 match (testo obsoleto rimosso)
    - `grep "sarà attivo nella Fase 2" skills/codestra-start-worker/SKILL.md` restituisce 0 match (nota futura rimossa)
    - `grep "workerPort" skills/codestra-start-worker/SKILL.md` restituisce almeno 2 match
    - `grep "callback_url" skills/codestra-start-worker/SKILL.md` restituisce almeno 1 match
    - `grep "^name:" skills/codestra-start-worker/SKILL.md` restituisce `name: codestra-start-worker` (frontmatter invariato)
    - `grep "^argument-hint:" skills/codestra-start-worker/SKILL.md` restituisce `argument-hint: [hub-ip] [hub-port] [worker-port?]` (frontmatter invariato)
    - `grep "swarm_register" skills/codestra-start-worker/SKILL.md` restituisce almeno 2 match (invariato — il tool è ancora referenziato)
  </acceptance_criteria>

  <done>SKILL.md aggiornato: $2 passa workerPort a swarm_register, istruzioni operative includono il parametro, output mostra callback_url. Le note placeholder della Fase 1 sono rimosse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrizione |
|----------|-------------|
| Utente → slash command $2 | `$2` è una stringa fornita dall'utente interpolata nel testo della skill |
| Testo skill → Claude (LLM) | Le istruzioni del SKILL.md guidano Claude a passare $2 come numero a swarm_register |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | `skills/codestra-start-worker/SKILL.md` (file locale) | accept | File sul filesystem locale dell'utente — stesso rischio accettato in T-01-01 (Fase 1). Plugin non pubblicato. |
| T-02-07 | Spoofing | `$2` come stringa non numerica passata a workerPort | mitigate | Il SKILL.md dice "passare il suo valore numerico" — Claude interpreta $2 come numero prima di passarlo. Il tool schema zod `z.number()` rifiuta stringhe non parsabili a numero. La coerce `Number(workerPort)` in mcp-server.mjs gestisce l'edge case. |
</threat_model>

<verification>
Verifiche finali dopo il task:

```bash
# 1. Testo obsoleto rimosso
grep "Ignorare per ora" skills/codestra-start-worker/SKILL.md
# Output atteso: nessun output (0 match)

grep "sarà attivo nella Fase 2" skills/codestra-start-worker/SKILL.md
# Output atteso: nessun output (0 match)

# 2. workerPort presente nelle istruzioni
grep "workerPort" skills/codestra-start-worker/SKILL.md
# Output atteso: almeno 2 righe

# 3. callback_url presente nell'output
grep "callback_url" skills/codestra-start-worker/SKILL.md
# Output atteso: almeno 1 riga

# 4. Frontmatter invariato
grep -E "^(name|argument-hint|disable-model-invocation):" skills/codestra-start-worker/SKILL.md
# Output atteso:
# name: codestra-start-worker
# argument-hint: [hub-ip] [hub-port] [worker-port?]
# disable-model-invocation: true
```
</verification>

<success_criteria>
1. `grep "Ignorare per ora" skills/codestra-start-worker/SKILL.md` restituisce 0 risultati
2. `grep "workerPort" skills/codestra-start-worker/SKILL.md` restituisce almeno 2 risultati
3. `grep "callback_url" skills/codestra-start-worker/SKILL.md` restituisce almeno 1 risultato
4. Il frontmatter (name, argument-hint, disable-model-invocation) è invariato
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/02-worker-http-server/02-02-SUMMARY.md` seguendo il template in `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
