---
phase: 08-skills-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/codestra-file-transport/SKILL.md
autonomous: true
requirements:
  - FILE-11

must_haves:
  truths:
    - "Il file `skills/codestra-file-transport/SKILL.md` esiste con frontmatter YAML valido"
    - "Il SKILL.md documenta la regola quando usare file transport vs message passing"
    - "Il SKILL.md documenta il limite testo-only ≤50 KB e la semantica ephemeral (restart hub = file persi)"
    - "Il SKILL.md include esempi concreti di chiamata per file_upload, file_download, file_list, file_delete"
    - "Il SKILL.md include la sequenza di test manuale two-worker step-by-step (Worker A upload → Worker B download, contenuto identico)"
  artifacts:
    - path: "skills/codestra-file-transport/SKILL.md"
      provides: "Guida operativa file transport per Claude worker"
      contains: "file_upload"
    - path: "skills/codestra-file-transport/SKILL.md"
      provides: "Integration test sequence documentata"
      contains: "Worker A"
  key_links:
    - from: "skills/codestra-file-transport/SKILL.md"
      to: "servers/mcp-server.mjs"
      via: "nomi tool MCP esatti (file_upload, file_download, file_list, file_delete)"
      pattern: "file_upload|file_download|file_list|file_delete"
---

<objective>
Creare `skills/codestra-file-transport/SKILL.md` — lo skill operativo che guida i Claude worker nell'uso del file transport MCP.

Purpose: I worker Codestra hanno bisogno di istruzioni chiare su quando e come usare i tool file_upload/download vs il message passing, con esempi concreti e un test di integrazione documentato.
Output: Un singolo file SKILL.md che soddisfa FILE-11 e i due success criteria del ROADMAP Phase 8.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-skills-integration/08-CONTEXT.md

<interfaces>
<!-- Tool MCP implementati live in servers/mcp-server.mjs (Phase 7). -->
<!-- Nomi e signature esatti — usare verbatim nel SKILL.md. -->

file_upload(filename, content, mimeType?)
  - filename: stringa — nome con cui archiviare il file (non un path filesystem)
  - content: stringa UTF-8 ≤~50 KB
  - mimeType: stringa opzionale (default: text/plain)
  - Risposta: { id, filename, size, mimeType, uploadedAt }

file_download(filename, offset?, max_bytes?)
  - filename: stringa — nome del file da scaricare
  - offset: numero opzionale — byte di partenza (default 0)
  - max_bytes: numero opzionale — bytes massimi da restituire (default 25000)
  - Risposta: { content, offset, total_size, has_more }
  - Se has_more: true → richiamare con offset = offset precedente + lunghezza content

file_list()
  - Nessun parametro
  - Risposta: [{ id, filename, size, mimeType, uploadedAt }, ...]

file_delete(filename)
  - filename: stringa — nome del file da eliminare
  - Risposta: { deleted: true } oppure { error: "..." }

Vincolo namespace: ogni tool opera sul namespace del worker registrato (registeredWorkerId).
Il worker DEVE essere registrato con swarm_register prima di usare qualsiasi file tool.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creare skills/codestra-file-transport/SKILL.md</name>
  <files>skills/codestra-file-transport/SKILL.md</files>

  <read_first>
    - `skills/codestra-messages/SKILL.md` — pattern frontmatter esatto (name, description, argument-hint, disable-model-invocation: true)
    - `skills/codestra-start-worker/SKILL.md` — struttura sezioni (Prerequisito, Istruzioni operative, Output all'utente)
    - `.planning/phases/08-skills-integration/08-CONTEXT.md` — decisioni D-01..D-04 bloccate
  </read_first>

  <action>
Creare la directory `skills/codestra-file-transport/` e scrivere `SKILL.md` con il contenuto seguente (verbatim — nessuna modifica all'interprete):

```
---
name: codestra-file-transport
description: Gestisce il trasporto di file nel namespace dello swarm Codestra. Supporta upload, download, lista e cancellazione file tramite MCP. Usa questo skill quando il payload supera qualche KB o deve essere condiviso tra più worker.
argument-hint: <azione> [filename] [contenuto]
disable-model-invocation: true
---

Gestisce il trasporto di file nel namespace di questo worker Codestra.

**Argomenti ricevuti:** $ARGUMENTS

Parametri:
- Azione (`$0`): operazione da eseguire — `upload`, `download`, `list`, `delete`.
- Filename (`$1`): nome del file (obbligatorio per upload, download, delete).
- Contenuto (`$2`): testo del file (obbligatorio solo per upload).

## Quando usare file transport vs message passing

**Usa `file_upload` / `file_download`** quando:
- Il payload supera qualche KB (testo lungo, report, output di elaborazione).
- Il contenuto deve essere accessibile a più worker contemporaneamente o in momenti diversi.
- Vuoi separare la notifica ("il file è pronto") dal trasferimento dati effettivo.

**Usa il message passing** (`swarm_send_message`) quando:
- Il payload è breve (conferme, comandi, ID, status update).
- La comunicazione è diretta tra due worker specifici e non richiede persistenza.
- Il contenuto ha senso solo nel momento in cui viene ricevuto.

**Regola pratica:** se il testo supera 2–3 righe o 500 caratteri, preferisci file transport.

## Limiti e semantica

- **Testo-only:** il contenuto deve essere una stringa UTF-8. File binari non supportati in v1.2.
- **Limite dimensione:** ≤ ~50 KB per invocazione MCP (circa 50.000 caratteri). File più grandi richiedono chunking manuale con `offset` / `max_bytes`.
- **Storage ephemeral:** i file sono conservati in memoria nell'hub. Un riavvio dell'hub cancella tutti i file. Non usare il file transport come storage permanente.
- **Namespace per worker:** ogni file è archiviato nel namespace del worker che lo ha caricato (identificato da `registeredWorkerId`). Il consumer deve conoscere il filename esatto e usare lo stesso namespace (stesso swarm).
- **Prerequisito:** il worker deve essere registrato (`swarm_register`) prima di usare qualsiasi file tool.

## Istruzioni operative

Se `$0` mancante o non riconosciuto, informa l'utente dei valori validi (`upload`, `download`, `list`, `delete`) e interrompi.

### Upload

Usa il tool `file_upload` con:
- `filename`: valore di `$1`
- `content`: valore di `$2` (il testo completo del file)
- `mimeType`: opzionale — ometti se non specificato (default `text/plain`)

Esempio input tool:
```json
{
  "filename": "report-analisi.txt",
  "content": "Risultati dell'analisi:\n- Item A: OK\n- Item B: WARN",
  "mimeType": "text/plain"
}
```

Risposta attesa (successo):
```json
{
  "id": "uuid-generato-hub",
  "filename": "report-analisi.txt",
  "size": 52,
  "mimeType": "text/plain",
  "uploadedAt": "2026-04-27T18:00:00.000Z"
}
```

### Download

Usa il tool `file_download` con:
- `filename`: valore di `$1`
- `offset`: opzionale — ometti per partire dall'inizio (default 0)
- `max_bytes`: opzionale — ometti per usare il default (25000 byte)

Esempio input tool:
```json
{
  "filename": "report-analisi.txt"
}
```

Risposta attesa (file completo in un chunk):
```json
{
  "content": "Risultati dell'analisi:\n- Item A: OK\n- Item B: WARN",
  "offset": 0,
  "total_size": 52,
  "has_more": false
}
```

Se `has_more` è `true`, il file è più grande del chunk restituito. Richiama `file_download` con `offset = offset_precedente + lunghezza_content` finché `has_more` è `false`.

### Lista file

Usa il tool `file_list` (nessun parametro).

Risposta attesa:
```json
[
  {
    "id": "uuid-generato-hub",
    "filename": "report-analisi.txt",
    "size": 52,
    "mimeType": "text/plain",
    "uploadedAt": "2026-04-27T18:00:00.000Z"
  }
]
```

Se il namespace è vuoto, la risposta è un array JSON vuoto `[]`.

### Delete

Usa il tool `file_delete` con:
- `filename`: valore di `$1`

Risposta attesa (successo):
```json
{ "deleted": true }
```

In caso di errore (file non trovato, worker non registrato), il tool restituisce `isError: true` con il messaggio dell'hub.

## Handoff two-worker: pattern produttore → consumatore

Il caso d'uso principale del file transport è che Worker A (produttore) carica un file e Worker B (consumatore) lo scarica usando lo stesso filename, coordinandosi tramite messaggi.

### Sequenza completa

**Worker A (produttore):**

1. Chiama `file_upload` con un filename concordato (es. `"handoff-risultato.txt"`) e il contenuto da trasferire.
2. Verifica che la risposta contenga `filename` e `uploadedAt` (nessun campo `error`).
3. Invia un messaggio a Worker B via `swarm_send_message` con body: `"file pronto: handoff-risultato.txt"`.

**Worker B (consumatore):**

4. Legge il messaggio di Worker A via `swarm_read_messages` (o riceve la notifica push).
5. Estrae il filename dal messaggio.
6. Chiama `file_download` con lo stesso filename: `"handoff-risultato.txt"`.
7. Verifica che `has_more` sia `false` (file ricevuto completo) oppure itera sui chunk.
8. Confronta il contenuto ricevuto con quello atteso — devono essere identici.

### Nota sul namespace

`file_download` opera sempre sul namespace di chi chiama il tool (il `registeredWorkerId` del worker che invoca). Pertanto Worker B scarica da **il proprio namespace**, non da quello di Worker A.

**Implicazione pratica:** per il pattern produttore → consumatore, Worker A e Worker B devono essere **registrati nello stesso swarm** (stesso `swarmId`). In questo caso condividono il namespace e `file_download` su Worker B trova il file caricato da Worker A.

Se i worker appartengono a swarm diversi, il file non è accessibile cross-swarm in v1.2.

## Test di integrazione manuale

Questa sequenza verifica il funzionamento end-to-end del file transport tra due istanze Claude Code. Tempo stimato: 3–5 minuti con due terminali aperti.

### Prerequisiti

- Hub Codestra avviato e raggiungibile (es. `http://localhost:7800`).
- Due istanze Claude Code configurate con lo stesso `SWARM_HUB_URL` e lo stesso `SWARM_ID` nel loro `.mcp.json`.
- Entrambe le istanze registrate (skill `codestra-start-worker` eseguito su ciascuna).

### Step 1 — Worker A: verifica stato iniziale

In Worker A, chiama `file_list`. Risposta attesa: array vuoto `[]` (o lista file esistenti — annotare).

### Step 2 — Worker A: carica il file di test

In Worker A, chiama `file_upload`:
```json
{
  "filename": "test-handoff.txt",
  "content": "CODESTRA_HANDOFF_TEST_v1\nTimestamp: <ora corrente>\nPayload: 42"
}
```
Risposta attesa: oggetto con `filename: "test-handoff.txt"` e `size` > 0. Annotare il valore di `size`.

### Step 3 — Worker A: conferma presenza file

In Worker A, chiama `file_list`. Il file `test-handoff.txt` deve apparire nell'array con lo stesso `size` annotato al passo 2.

### Step 4 — Worker A: notifica Worker B

In Worker A, chiama `swarm_send_message` verso Worker B con body:
`"file pronto: test-handoff.txt"`

### Step 5 — Worker B: scarica il file

In Worker B (istanza separata), chiama `file_download`:
```json
{
  "filename": "test-handoff.txt"
}
```
Risposta attesa:
- `content` = stringa identica a quella caricata al passo 2
- `has_more: false`
- `total_size` = stesso valore di `size` annotato al passo 2

### Step 6 — Verifica contenuto identico

Confronta il campo `content` della risposta di `file_download` con il testo originale caricato al passo 2. Devono corrispondere carattere per carattere (inclusi `\n`).

### Step 7 — Pulizia (opzionale)

In Worker A, chiama `file_delete`:
```json
{ "filename": "test-handoff.txt" }
```
Risposta attesa: `{ "deleted": true }`.

### Criteri di superamento

- [ ] Passo 2: `file_upload` restituisce metadata senza `error`
- [ ] Passo 3: `file_list` contiene `test-handoff.txt`
- [ ] Passo 5: `file_download` restituisce `has_more: false`
- [ ] Passo 6: contenuto ricevuto da Worker B = contenuto caricato da Worker A
- [ ] Passo 7: `file_delete` restituisce `{ "deleted": true }`

## Output all'utente

Al termine dell'operazione, mostra:
- **Upload:** "File `<filename>` caricato. Dimensione: `<size>` byte. ID hub: `<id>`."
- **Download:** "File `<filename>` scaricato (`<total_size>` byte totali). Contenuto:" seguito dal testo.
- **List:** elenco dei file con nome, dimensione e data upload. Se vuoto: "Nessun file nel namespace."
- **Delete:** "File `<filename>` eliminato."
```
  </action>

  <verify>
    <automated>test -f /home/g/Documenti/codestra/skills/codestra-file-transport/SKILL.md && grep -q "file_upload" /home/g/Documenti/codestra/skills/codestra-file-transport/SKILL.md && grep -q "file_download" /home/g/Documenti/codestra/skills/codestra-file-transport/SKILL.md && grep -q "file_list" /home/g/Documenti/codestra/skills/codestra-file-transport/SKILL.md && grep -q "file_delete" /home/g/Documenti/codestra/skills/codestra-file-transport/SKILL.md && grep -q "disable-model-invocation: true" /home/g/Documenti/codestra/skills/codestra-file-transport/SKILL.md && grep -q "Worker A" /home/g/Documenti/codestra/skills/codestra-file-transport/SKILL.md && grep -q "50 KB" /home/g/Documenti/codestra/skills/codestra-file-transport/SKILL.md && grep -q "ephemeral\|riavvio" /home/g/Documenti/codestra/skills/codestra-file-transport/SKILL.md && echo "OK"</automated>
  </verify>

  <acceptance_criteria>
    - `skills/codestra-file-transport/SKILL.md` esiste
    - `grep -q "disable-model-invocation: true"` passa
    - `grep -q "name: codestra-file-transport"` passa
    - `grep -q "file_upload"` passa
    - `grep -q "file_download"` passa
    - `grep -q "file_list"` passa
    - `grep -q "file_delete"` passa
    - `grep -q "50 KB"` passa (limite dimensione documentato)
    - `grep -q "ephemeral\|riavvio"` passa (semantica ephemeral documentata)
    - `grep -q "Worker A"` passa (sequenza handoff two-worker presente)
    - `grep -q "test-handoff.txt"` passa (test di integrazione manuale presente)
    - `grep -q "has_more"` passa (paginazione documentata)
    - Frontmatter YAML valido: `name`, `description`, `argument-hint`, `disable-model-invocation` presenti
    - Testo in italiano (nessuna sezione in inglese salvo esempi JSON/code)
  </acceptance_criteria>

  <done>Il file `skills/codestra-file-transport/SKILL.md` esiste, supera tutti i grep di verifica, e documenta: regola when-to-use, limiti (50 KB, text-only, ephemeral), esempi tool call con input/output attesi, pattern handoff two-worker, sequenza di test manuale in 7 step con criteri di superamento espliciti.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrizione |
|----------|-------------|
| Worker → Hub | Il SKILL.md istruisce il worker a chiamare tool MCP che proxy verso l'hub HTTP. L'hub è il confine di trust. |
| Filename come input untrusted | Il filename arriva come argomento `$1` dal caller — potenziale injection se usato in path filesystem (non applicabile qui: hub tratta filename come metadato opaco). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Spoofing | file_download — namespace del caller | accept | Il namespace è legato a `registeredWorkerId` (assegnato dall'hub al momento della registrazione); un worker non può impersonare un altro senza credenziali hub. |
| T-08-02 | Tampering | Contenuto file in transito | accept | Comunicazione hub-to-MCP è localhost o rete interna (stesso swarm); TLS out of scope v1.2, rischio accettato. |
| T-08-03 | Repudiation | Nessun audit log upload/download | accept | Non richiesto in v1.2; l'hub registra `uploadedAt` ma non access log. |
| T-08-04 | Information Disclosure | File nel namespace accessibile a tutti i worker dello stesso swarm | mitigate | Il SKILL.md documenta esplicitamente che il namespace è condiviso nello swarm — il worker deve usare filename non-indovinabili per contenuto sensibile. |
| T-08-05 | Denial of Service | Upload ripetuti saturano la memoria hub | accept | Limite 10 MB hub-side già implementato in Phase 6 (413 response); limite 50 KB per invocazione MCP documentato nel SKILL.md. |
| T-08-06 | Elevation of Privilege | Filename con path traversal (es. `../../etc/passwd`) | accept | L'hub tratta il filename come chiave di metadato opaca in una Map in-memory — nessuna operazione filesystem coinvolta, path traversal non applicabile. |
</threat_model>

<verification>
```bash
# 1. File esiste
test -f /home/g/Documenti/codestra/skills/codestra-file-transport/SKILL.md && echo "EXISTS: OK"

# 2. Frontmatter obbligatori
grep -q "name: codestra-file-transport" skills/codestra-file-transport/SKILL.md && echo "name: OK"
grep -q "disable-model-invocation: true" skills/codestra-file-transport/SKILL.md && echo "disable-model-invocation: OK"

# 3. Tutti e quattro i tool documentati
for tool in file_upload file_download file_list file_delete; do
  grep -q "$tool" skills/codestra-file-transport/SKILL.md && echo "$tool: OK"
done

# 4. Contenuti obbligatori per FILE-11
grep -q "50 KB" skills/codestra-file-transport/SKILL.md && echo "limite 50KB: OK"
grep -q "ephemeral\|riavvio" skills/codestra-file-transport/SKILL.md && echo "semantica ephemeral: OK"
grep -q "has_more" skills/codestra-file-transport/SKILL.md && echo "paginazione: OK"
grep -q "Worker A" skills/codestra-file-transport/SKILL.md && echo "handoff two-worker: OK"
grep -q "test-handoff.txt" skills/codestra-file-transport/SKILL.md && echo "test integrazione: OK"

# 5. Naming convention directory
test -d /home/g/Documenti/codestra/skills/codestra-file-transport && echo "directory naming: OK"
```
</verification>

<success_criteria>
- `skills/codestra-file-transport/SKILL.md` esiste con frontmatter YAML valido (name, description, argument-hint, disable-model-invocation: true)
- Documenta quando usare file transport vs message passing (regola esplicita)
- Documenta: limite ≤50 KB text-only, semantica ephemeral (riavvio hub = file persi), namespace per swarm
- Include esempi JSON di input/output per tutti e quattro i tool (file_upload, file_download, file_list, file_delete)
- Include la sequenza di test manuale two-worker in 7 step con criteri di superamento grep-verificabili
- Testo in italiano, coerente con gli skill esistenti nel progetto
- Tutti i grep di verifica nella sezione `<verify>` ritornano "OK"
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/08-skills-integration/08-01-SUMMARY.md` seguendo il template in `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
