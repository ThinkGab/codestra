---
phase: 01-slash-command-skills
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/plugin.json
  - skills/codestra-start-hub/SKILL.md
  - skills/codestra-start-worker/SKILL.md
autonomous: true
requirements:
  - CMD-01
  - CMD-02

must_haves:
  truths:
    - "L'utente può digitare /codestra:codestra-start-hub e Claude Code presenta il suggerimento [port] [ip]"
    - "L'utente può digitare /codestra:codestra-start-worker e Claude Code presenta il suggerimento [hub-ip] [hub-port] [worker-port?]"
    - "La skill hub istruisce Claude a usare swarm_hub_start per port, e bash cmd con SWARM_HOST per ip"
    - "La skill worker istruisce Claude a verificare SWARM_HUB_URL e invocare swarm_register con role=worker"
    - "Il file plugin.jsons non esiste più nel repository"
    - "Il namespace del plugin è 'codestra' (non 'claude-swarm')"
  artifacts:
    - path: ".claude-plugin/plugin.json"
      provides: "Manifest del plugin con name: codestra"
      contains: '"name": "codestra"'
    - path: "skills/codestra-start-hub/SKILL.md"
      provides: "Skill per avviare l'hub"
      exports: ["frontmatter con name/description/argument-hint/disable-model-invocation", "istruzioni per swarm_hub_start e bash cmd"]
    - path: "skills/codestra-start-worker/SKILL.md"
      provides: "Skill per registrare il worker"
      exports: ["frontmatter con name/description/argument-hint/disable-model-invocation", "istruzioni per swarm_register"]
  key_links:
    - from: "skills/codestra-start-hub/SKILL.md"
      to: "servers/mcp-server.mjs → swarm_hub_start"
      via: "istruzioni nel corpo della skill"
      pattern: "swarm_hub_start"
    - from: "skills/codestra-start-worker/SKILL.md"
      to: "servers/mcp-server.mjs → swarm_register"
      via: "istruzioni nel corpo della skill"
      pattern: "swarm_register"
    - from: ".claude-plugin/plugin.json"
      to: "skills/codestra-start-hub/ e skills/codestra-start-worker/"
      via: "autodiscovery di Claude Code sulla directory skills/"
      pattern: '"name": "codestra"'
---

<objective>
Rendere operativi i due slash command /codestra:codestra-start-hub e /codestra:codestra-start-worker nel plugin Codestra.

Purpose: Gli utenti devono poter invocare i comandi con argomenti posizionali e ricevere istruzioni chiare su come Claude deve avviare l'hub o registrare il worker via MCP.

Output:
- .claude-plugin/plugin.json aggiornato con name "codestra" (rebrand da "claude-swarm")
- skills/codestra-start-hub/SKILL.md con frontmatter completo e istruzioni operative
- skills/codestra-start-worker/SKILL.md con frontmatter completo e istruzioni operative
- .claude-plugin/plugin.jsons eliminato (file duplicato con typo nell'estensione)
- Directory skills/ rinominate da orchestrate/messaging ai nomi finali del comando
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-slash-command-skills/01-RESEARCH.md
@.planning/phases/01-slash-command-skills/01-PATTERNS.md

<interfaces>
<!-- Tool MCP disponibili in servers/mcp-server.mjs — l'esecutore NON deve esplorare il codebase. -->

swarm_hub_start (righe 44-73 di mcp-server.mjs):
  Parametri: port (number, optional, default 7800), secret (string, optional)
  NON accetta: host/ip — hub.mjs legge SWARM_HOST da env var
  Comportamento: restituisce un comando bash che Claude deve eseguire via Bash tool
  Esempio output: "SWARM_PORT=8080 nohup node /path/to/hub.mjs > /tmp/swarm-hub.log 2>&1 &"

swarm_register (righe 98-126 di mcp-server.mjs):
  Parametri: role (enum "leader"|"worker", optional), task (string, optional)
  NON accetta: hub_url/hub_ip/hub_port — mcp-server.mjs legge SWARM_HUB_URL da env var
  Comportamento: POST a HUB_URL/workers con body {role, task, cwd}

Env var critiche:
  SWARM_HOST  → letto da hub.mjs per binding IP (default "0.0.0.0")
  SWARM_PORT  → letto da hub.mjs per porta (default 7800)
  SWARM_HUB_URL → letto da mcp-server.mjs per URL dell'hub (default "http://localhost:7800")
  SWARM_ROLE  → letto da mcp-server.mjs per ruolo del worker (default "worker")
  SWARM_SECRET → letto da entrambi per autenticazione (default "")

plugin.json attuale:
{
  "name": "claude-swarm",        ← DA CAMBIARE in "codestra"
  "version": "0.1.0",
  "description": "Orchestrate multiple Claude Code instances via MCP with Hub & Spoke architecture. Spawn workers, send messages, and coordinate tasks across LAN.",
  "author": { "name": "Gabriele Di Lelio" },
  "keywords": ["orchestration", "multi-agent", "swarm", "mcp", "networking"],
  "mcpServers": "./.mcp.json"
}

Directory skills/ attuale:
  skills/orchestrate/SKILL.md   ← stub vuoto, va rinominata in codestra-start-hub/
  skills/messaging/SKILL.md     ← stub vuoto, va rinominata in codestra-start-worker/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rinomina directory skill, aggiorna manifest, elimina file duplicato</name>
  <files>.claude-plugin/plugin.json</files>

  <read_first>
    - .claude-plugin/plugin.json (file da modificare — leggere prima di scrivere)
    - .claude-plugin/plugin.jsons (confermare identico a plugin.json prima di eliminare)
    - skills/orchestrate/SKILL.md (confermare che sia stub vuoto prima di rinominare)
    - skills/messaging/SKILL.md (confermare che sia stub vuoto prima di rinominare)
  </read_first>

  <action>
Eseguire queste operazioni nell'ordine indicato:

**1. Rinomina directory skill con git mv (preserva history):**
```bash
git mv skills/orchestrate skills/codestra-start-hub
git mv skills/messaging skills/codestra-start-worker
```

**2. Elimina il file duplicato plugin.jsons:**
```bash
git rm .claude-plugin/plugin.jsons
```

**3. Aggiorna .claude-plugin/plugin.json — cambia SOLO il campo `name`:**

Contenuto finale del file (copia esatta da scrivere):
```json
{
  "name": "codestra",
  "version": "0.1.0",
  "description": "Orchestrate multiple Claude Code instances via MCP with Hub & Spoke architecture. Spawn workers, send messages, and coordinate tasks across LAN.",
  "author": { "name": "Gabriele Di Lelio" },
  "keywords": ["orchestration", "multi-agent", "swarm", "mcp", "networking"],
  "mcpServers": "./.mcp.json"
}
```

**Razionale della decisione `name: "codestra"`:**
Il progetto ha subito un rebrand da "claude-swarm" a "Codestra". Aggiornare il namespace ora ha rischio zero (nessun utente reale, nessuna installazione in produzione). I comandi risultanti saranno `/codestra:codestra-start-hub` e `/codestra:codestra-start-worker` — coerenti con il brand. Se il nome fosse rimasto "claude-swarm", i comandi sarebbero stati `/claude-swarm:codestra-start-hub` — branding incoerente.

**Claude Code autodiscopre `skills/` automaticamente:** Non è necessario aggiungere nessun campo `skills` al manifest. La sola directory `skills/` alla radice del plugin è sufficiente.
  </action>

  <verify>
```bash
# 1. Directory rinominate correttamente
ls /home/g/Documenti/codestra/skills/
# Output atteso: codestra-start-hub  codestra-start-worker

# 2. File duplicato eliminato
ls /home/g/Documenti/codestra/.claude-plugin/
# Output atteso: solo plugin.json (nessun plugin.jsons)

# 3. Manifest aggiornato con nome corretto
grep '"name"' /home/g/Documenti/codestra/.claude-plugin/plugin.json
# Output atteso: "name": "codestra"

# 4. Git staging corretto
git -C /home/g/Documenti/codestra status --short
# Output atteso: R  skills/orchestrate/SKILL.md -> skills/codestra-start-hub/SKILL.md
#                R  skills/messaging/SKILL.md -> skills/codestra-start-worker/SKILL.md
#                D  .claude-plugin/plugin.jsons
#                M  .claude-plugin/plugin.json
```
  </verify>

  <acceptance_criteria>
    - `ls skills/` mostra esattamente `codestra-start-hub` e `codestra-start-worker` (non `orchestrate` e `messaging`)
    - `ls .claude-plugin/` mostra solo `plugin.json` (nessun `plugin.jsons`)
    - `grep '"name"' .claude-plugin/plugin.json` restituisce `"name": "codestra"`
    - `grep '"version"' .claude-plugin/plugin.json` restituisce `"version": "0.1.0"` (nessun altro campo modificato)
    - `git status` mostra i 4 cambiamenti attesi (2 rename, 1 delete, 1 modify) — nessun file inatteso nello staging
  </acceptance_criteria>

  <done>Directory skill hanno i nomi finali del comando. Manifest usa namespace "codestra". File duplicato plugin.jsons eliminato da git.</done>
</task>

<task type="auto">
  <name>Task 2: Scrivi contenuto completo di entrambi i SKILL.md</name>
  <files>
    skills/codestra-start-hub/SKILL.md
    skills/codestra-start-worker/SKILL.md
  </files>

  <read_first>
    - skills/codestra-start-hub/SKILL.md (verificare che sia stub vuoto prima di sovrascrivere)
    - skills/codestra-start-worker/SKILL.md (verificare che sia stub vuoto prima di sovrascrivere)
    - .planning/phases/01-slash-command-skills/01-RESEARCH.md (sezione "Code Examples" per pattern SKILL.md)
    - .planning/phases/01-slash-command-skills/01-PATTERNS.md (sezione "Pattern 3: Gap tool MCP" per gestione argomenti)
  </read_first>

  <action>
**Scrivere esattamente questo contenuto in `skills/codestra-start-hub/SKILL.md`:**

```markdown
---
name: codestra-start-hub
description: Avvia il Swarm Hub Codestra su questa istanza Claude Code. Usare quando si vuole essere il nodo centrale dello swarm. Gli altri Claude Code si registreranno come worker su questo hub.
argument-hint: [port] [ip]
disable-model-invocation: true
---

Avvia il Swarm Hub Codestra su questa istanza.

**Argomenti ricevuti:** $ARGUMENTS

Parametri:
- Porta (`$0`): porta su cui l'hub ascolta le connessioni. Default: 7800 se non specificato.
- IP binding (`$1`): indirizzo IP su cui l'hub si lega. Default: 0.0.0.0 (tutte le interfacce, accesso LAN). Specificare un IP esplicito per restringere l'accesso a una sola interfaccia.

## Istruzioni operative

**Caso A — Solo porta specificata (o nessun argomento):**
Usa il tool `swarm_hub_start` con `port=$0`.
- Se nessun argomento: chiama `swarm_hub_start` senza parametri (usa default 7800).
- Se solo `$0` fornito: chiama `swarm_hub_start` con `port=$0`.
- Il tool restituisce un comando bash: eseguilo via Bash tool.

**Caso B — Porta e IP specificati ($1 presente):**
Il tool `swarm_hub_start` non supporta il parametro host direttamente — hub.mjs legge `SWARM_HOST` da env var.
Costruisci e lancia il comando bash direttamente:

```bash
SWARM_HOST=$1 SWARM_PORT=$0 nohup node "${CLAUDE_SKILL_DIR}/../../servers/hub.mjs" > /tmp/swarm-hub.log 2>&1 &
```

Se `$0` non è specificato, ometti `SWARM_PORT` (verrà usato il default 7800).

## Verifica avvio

Dopo aver eseguito il comando bash, attendi 1 secondo e verifica con:

```bash
curl http://localhost:${0:-7800}/health
```

Risposta attesa: `{"status":"ok"}` o simile JSON con stato dell'hub.

## Output all'utente

Mostra all'utente:
- L'URL dell'hub: `http://<ip-o-localhost>:<porta>`
- Come i worker possono registrarsi: `SWARM_HUB_URL=http://<ip>:<porta>` da impostare nel loro `.mcp.json`
- Il log dell'hub: `/tmp/swarm-hub.log`
```

---

**Scrivere esattamente questo contenuto in `skills/codestra-start-worker/SKILL.md`:**

```markdown
---
name: codestra-start-worker
description: Registra questa istanza Claude Code come worker nel Swarm Hub Codestra. Specificare l'indirizzo IP e la porta dell'hub. Il worker-port è opzionale (sarà utilizzato nella Fase 2 per comunicazione push bidirezionale).
argument-hint: [hub-ip] [hub-port] [worker-port?]
disable-model-invocation: true
---

Registra questa istanza Claude Code come worker nel Swarm Hub Codestra.

**Argomenti ricevuti:** $ARGUMENTS

Parametri:
- Hub IP (`$0`): indirizzo IP dell'istanza hub (es. `192.168.1.10` o `localhost`).
- Hub Port (`$1`): porta dell'hub (es. `7800`). Default: 7800 se non specificato.
- Worker Port (`$2`): opzionale — porta per il server HTTP del worker (Fase 2). Ignorare per ora se fornito.

## Prerequisito

Il tool `swarm_register` si connette all'hub tramite `SWARM_HUB_URL`, che viene letta dalla variabile d'ambiente al momento dell'avvio del MCP server — non è un parametro runtime del tool.

Prima di invocare `swarm_register`, verifica che `SWARM_HUB_URL` nel file `.mcp.json` di questa istanza punti a `http://$0:${1:-7800}`. Se non corrisponde, informa l'utente e chiedi di aggiornare `.mcp.json` e riavviare Claude Code, oppure segui le istruzioni nel prossimo paragrafo.

**Se SWARM_HUB_URL non è configurata o punta all'hub sbagliato:**
Informa l'utente che deve aggiornare `.mcp.json`:
```json
{
  "mcpServers": {
    "codestra": {
      "env": {
        "SWARM_HUB_URL": "http://$0:${1:-7800}"
      }
    }
  }
}
```
Dopo l'aggiornamento, l'utente deve riavviare Claude Code per applicare la nuova env var.

## Istruzioni operative

Una volta verificato che `SWARM_HUB_URL` punta all'hub corretto:

1. Usa il tool `swarm_register` con:
   - `role`: `"worker"`
   - `task`: breve descrizione del lavoro che questo worker svolgerà (chiedere all'utente se non noto)

2. Verifica la risposta del tool — deve contenere un worker ID assegnato dall'hub.

## Output all'utente

Mostra all'utente:
- Il worker ID assegnato dall'hub
- L'URL dell'hub a cui è connesso: `http://$0:${1:-7800}`
- Conferma che il worker è ora registrato e in ascolto di task
- Nota: il parametro worker-port (`$2`) sarà attivo nella Fase 2 (Worker HTTP Server)
```
  </action>

  <verify>
```bash
# 1. Frontmatter hub: tutti i campi obbligatori presenti
grep -E "^name:|^description:|^argument-hint:|^disable-model-invocation:" \
  /home/g/Documenti/codestra/skills/codestra-start-hub/SKILL.md
# Output atteso (4 righe):
# name: codestra-start-hub
# description: Avvia il Swarm Hub...
# argument-hint: [port] [ip]
# disable-model-invocation: true

# 2. Frontmatter worker: tutti i campi obbligatori presenti
grep -E "^name:|^description:|^argument-hint:|^disable-model-invocation:" \
  /home/g/Documenti/codestra/skills/codestra-start-worker/SKILL.md
# Output atteso (4 righe):
# name: codestra-start-worker
# description: Registra questa istanza...
# argument-hint: [hub-ip] [hub-port] [worker-port?]
# disable-model-invocation: true

# 3. Hub skill referenzia swarm_hub_start
grep "swarm_hub_start" /home/g/Documenti/codestra/skills/codestra-start-hub/SKILL.md
# Output atteso: almeno 1 riga con "swarm_hub_start"

# 4. Worker skill referenzia swarm_register
grep "swarm_register" /home/g/Documenti/codestra/skills/codestra-start-worker/SKILL.md
# Output atteso: almeno 1 riga con "swarm_register"

# 5. Hub skill gestisce il caso ip ($1) con bash cmd
grep "SWARM_HOST" /home/g/Documenti/codestra/skills/codestra-start-hub/SKILL.md
# Output atteso: almeno 1 riga (il workaround per il gap del tool MCP)

# 6. Worker skill documenta il prerequisito SWARM_HUB_URL
grep "SWARM_HUB_URL" /home/g/Documenti/codestra/skills/codestra-start-worker/SKILL.md
# Output atteso: almeno 2 righe

# 7. Nessun SKILL.md è ancora stub vuoto
wc -l /home/g/Documenti/codestra/skills/codestra-start-hub/SKILL.md \
       /home/g/Documenti/codestra/skills/codestra-start-worker/SKILL.md
# Output atteso: entrambi > 10 righe
```
  </verify>

  <acceptance_criteria>
    - `grep "^name:" skills/codestra-start-hub/SKILL.md` restituisce `name: codestra-start-hub`
    - `grep "^name:" skills/codestra-start-worker/SKILL.md` restituisce `name: codestra-start-worker`
    - `grep "^disable-model-invocation:" skills/codestra-start-hub/SKILL.md` restituisce `disable-model-invocation: true`
    - `grep "^disable-model-invocation:" skills/codestra-start-worker/SKILL.md` restituisce `disable-model-invocation: true`
    - `grep "^argument-hint:" skills/codestra-start-hub/SKILL.md` restituisce `argument-hint: [port] [ip]`
    - `grep "^argument-hint:" skills/codestra-start-worker/SKILL.md` restituisce `argument-hint: [hub-ip] [hub-port] [worker-port?]`
    - `grep "swarm_hub_start" skills/codestra-start-hub/SKILL.md` restituisce almeno 1 match
    - `grep "swarm_register" skills/codestra-start-worker/SKILL.md` restituisce almeno 1 match
    - `grep "SWARM_HOST" skills/codestra-start-hub/SKILL.md` restituisce almeno 1 match (gestione gap ip)
    - `grep "SWARM_HUB_URL" skills/codestra-start-worker/SKILL.md` restituisce almeno 2 match (prerequisito documentato)
    - Entrambi i file superano `wc -l` > 10 righe (non sono stub vuoti)
  </acceptance_criteria>

  <done>Entrambe le SKILL.md hanno frontmatter completo e istruzioni operative. La skill hub gestisce il gap dell'argomento ip con dual-path (tool MCP per port, bash cmd per SWARM_HOST). La skill worker documenta il prerequisito SWARM_HUB_URL e come aggiornarlo se necessario.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrizione |
|----------|-------------|
| File system locale → Claude Code | SKILL.md letti da Claude Code dal filesystem locale del plugin installato |
| Claude Code → Claude (LLM) | Il contenuto della SKILL.md viene inviato come prompt a Claude dopo l'interpolazione degli argomenti |
| Utente → slash command | L'utente fornisce argomenti posizionali ($0, $1, $2) che vengono interpolati nel testo della skill |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `skills/*/SKILL.md` (file locali) | accept | I file sono sul filesystem locale dell'utente che ha già accesso completo. Nessun vettore di attacco esterno in questa fase — plugin non pubblicato, nessun utente remoto. |
| T-01-02 | Spoofing | Argomenti posizionali `$0`, `$1`, `$2` | accept | Claude Code interpola gli argomenti prima che Claude veda il testo. Non c'è autenticazione degli argomenti, ma il contesto è LAN locale e l'utente controlla il comando. Per v1.0 LAN use case il rischio è accettabile. |
| T-01-03 | Information Disclosure | `disable-model-invocation: true` assente | mitigate | Entrambe le SKILL.md devono avere `disable-model-invocation: true` nel frontmatter. Verificato nei `<acceptance_criteria>` del Task 2. Senza questo campo, Claude potrebbe auto-invocare comandi con side-effects (avvio server) senza consenso esplicito dell'utente. |
| T-01-04 | Elevation of Privilege | Comando bash con `nohup node hub.mjs` costruito dalla skill | accept | Il comando viene eseguito con i permessi dell'utente corrente — nessuna elevazione. L'utente ha già accesso al filesystem. Il path di hub.mjs è risolto tramite `${CLAUDE_SKILL_DIR}` (path del plugin) — non è user-controllable. |
| T-01-05 | Denial of Service | `plugin.jsons` duplicato lasciato in repo | mitigate | Eliminato in Task 1 via `git rm`. Presenza di due manifest potrebbe causare comportamento non deterministico del plugin loader. |
</threat_model>

<verification>
Verifiche finali dopo entrambi i task:

```bash
# Struttura completa attesa
find /home/g/Documenti/codestra/skills /home/g/Documenti/codestra/.claude-plugin \
  -type f | sort
# Output atteso:
# .claude-plugin/plugin.json
# skills/codestra-start-hub/SKILL.md
# skills/codestra-start-worker/SKILL.md

# Namespace del plugin
grep '"name"' /home/g/Documenti/codestra/.claude-plugin/plugin.json
# Output atteso: "name": "codestra"

# Entrambe le skill hanno disable-model-invocation
grep "disable-model-invocation: true" \
  /home/g/Documenti/codestra/skills/codestra-start-hub/SKILL.md \
  /home/g/Documenti/codestra/skills/codestra-start-worker/SKILL.md
# Output atteso: 2 righe (una per file)

# Git status pulito dopo commit
git -C /home/g/Documenti/codestra status
# Output atteso: nothing to commit, working tree clean
```
</verification>

<success_criteria>
1. `/codestra:codestra-start-hub` è un comando valido in Claude Code — la directory `skills/codestra-start-hub/` esiste con SKILL.md completo
2. `/codestra:codestra-start-worker` è un comando valido in Claude Code — la directory `skills/codestra-start-worker/` esiste con SKILL.md completo
3. Entrambe le skill mostrano `[port] [ip]` e `[hub-ip] [hub-port] [worker-port?]` nell'autocomplete (argument-hint nel frontmatter)
4. Invocare `/codestra:codestra-start-hub 8080 192.168.1.5` fa sì che Claude costruisca `SWARM_HOST=192.168.1.5 SWARM_PORT=8080 nohup node .../hub.mjs > /tmp/swarm-hub.log 2>&1 &`
5. Invocare `/codestra:codestra-start-hub 8080` fa sì che Claude usi `swarm_hub_start` con `port=8080`
6. Invocare `/codestra:codestra-start-worker 192.168.1.10 7800` fa sì che Claude verifichi `SWARM_HUB_URL` e invochi `swarm_register` con `role=worker`
7. Il file `.claude-plugin/plugin.jsons` non esiste nel repository
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/01-slash-command-skills/01-01-SUMMARY.md` seguendo il template in `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
